# Question Bank: JavaScript
## UCS542: UI & UX SPECIALIST

---

## Topic 1: JavaScript Basics

### Conceptual Questions
1. What is JavaScript and what can it do?
2. Explain the difference between JavaScript and Java.
3. What are the different ways to include JavaScript in HTML?
4. What is the difference between client-side and server-side JavaScript?
5. How does JavaScript execute in the browser?

### Practical Questions
6. Write a JavaScript program to display "Hello World" in console.
7. Create a script that shows an alert message when page loads.
8. Demonstrate the use of `console.log()`, `console.warn()`, and `console.error()`.
9. Write JavaScript code to change HTML content dynamically.
10. Create a script that displays current date and time.

---

## Topic 2: ES6 Specifications & Features

### Conceptual Questions
11. What is ECMAScript and how is it related to JavaScript?
12. What are the major features introduced in ES6?
13. Explain the difference between `var`, `let`, and `const`.
14. What is block scope in ES6?
15. What is hoisting in JavaScript?

### Practical Questions
16. Demonstrate the difference between `var`, `let`, and `const` with examples.
17. Create a program showing block scope vs function scope.
18. Write code to show temporal dead zone with `let` and `const`.
19. Demonstrate hoisting with `var` and function declarations.
20. Create examples showing when to use `let` vs `const`.

### Advanced Questions
21. Explain the concept of template literals and their advantages.
22. What are tagged template literals?
23. Demonstrate the use of default parameters in ES6.
24. Explain rest parameters and how they differ from arguments object.
25. What is the difference between `==` and `===`?

---

## Topic 3: Variables & Data Types

### Conceptual Questions
26. What are the primitive data types in JavaScript?
27. Explain the difference between primitive and reference types.
28. What is `undefined` vs `null`?
29. What is type coercion in JavaScript?
30. How does JavaScript handle dynamic typing?

### Practical Questions
31. Create variables of all primitive data types.
32. Demonstrate type coercion with examples.
33. Write code to check the type of a variable using `typeof`.
34. Show the difference between `null` and `undefined`.
35. Create a program that converts string to number and vice versa.

### Advanced Questions
36. Explain `NaN` and how to check for it.
37. What is the difference between `Number()`, `parseInt()`, and `parseFloat()`?
38. Demonstrate truthy and falsy values in JavaScript.
39. Explain Symbol data type and its use cases.
40. What is BigInt and when should it be used?

---

## Topic 4: Operators

### Conceptual Questions
41. What are the different types of operators in JavaScript?
42. Explain arithmetic operators with examples.
43. What are comparison operators and their return values?
44. Explain logical operators (&&, ||, !).
45. What is the ternary operator?

### Practical Questions
46. Create a calculator using arithmetic operators.
47. Write a program using comparison operators to compare two numbers.
48. Demonstrate the use of logical operators in conditions.
49. Use ternary operator to check if a number is even or odd.
50. Create a program using increment and decrement operators.

### Advanced Questions
51. Explain the nullish coalescing operator (??).
52. What is the optional chaining operator (?.) and its use?
53. Demonstrate short-circuit evaluation with && and ||.
54. Explain the difference between `++i` and `i++`.
55. What are bitwise operators and when are they used?

---

## Topic 5: JavaScript Functions

### Conceptual Questions
56. What is a function in JavaScript?
57. Explain the difference between function declaration and function expression.
58. What are anonymous functions?
59. What is an IIFE (Immediately Invoked Function Expression)?
60. Explain function scope and closure.

### Practical Questions
61. Create a function that adds two numbers.
62. Write a function expression and call it.
63. Demonstrate an IIFE with an example.
64. Create a function with default parameters.
65. Write a function that returns another function (closure).

### Advanced Questions
66. Explain arrow functions and their syntax.
67. What is the difference between regular functions and arrow functions?
68. How does `this` behave differently in arrow functions?
69. Create a higher-order function that takes a function as argument.
70. Demonstrate function currying with examples.

---

## Topic 6: Arrow Functions

### Conceptual Questions
71. What are arrow functions in ES6?
72. What are the advantages of arrow functions?
73. When should you NOT use arrow functions?
74. How do arrow functions handle `this` keyword?
75. Can arrow functions be used as constructors?

### Practical Questions
76. Convert a regular function to arrow function.
77. Create an arrow function with implicit return.
78. Write an arrow function with multiple parameters.
79. Demonstrate arrow function in array methods.
80. Create an arrow function with no parameters.

### Advanced Questions
81. Explain lexical `this` in arrow functions.
82. Show the difference in `this` binding between regular and arrow functions.
83. Create a method using arrow function and explain the issue.
84. Demonstrate arrow functions in callbacks.
85. Use arrow functions with array methods (map, filter, reduce).

---

## Topic 7: Objects & Destructuring

### Conceptual Questions
86. What are objects in JavaScript?
87. How do you create objects in JavaScript?
88. What is object destructuring in ES6?
89. Explain the difference between dot notation and bracket notation.
90. What are object methods?

### Practical Questions
91. Create an object representing a person with properties and methods.
92. Demonstrate object destructuring with examples.
93. Use destructuring to extract values from nested objects.
94. Create an object with computed property names.
95. Write code to merge two objects using spread operator.

### Advanced Questions
96. Explain object shorthand property and method syntax.
97. What is the difference between shallow copy and deep copy?
98. Demonstrate Object.keys(), Object.values(), and Object.entries().
99. Create a program using Object.assign() and spread operator.
100. Explain the concept of object references and mutability.

---

## Topic 8: Spread Operator

### Conceptual Questions
101. What is the spread operator (...) in ES6?
102. How does spread operator work with arrays?
103. How does spread operator work with objects?
104. What is the difference between spread and rest operator?
105. Can spread operator be used with strings?

### Practical Questions
106. Use spread operator to combine two arrays.
107. Create a copy of an array using spread operator.
108. Merge two objects using spread operator.
109. Use spread operator to pass array elements as function arguments.
110. Demonstrate spreading a string into individual characters.

### Advanced Questions
111. Create a function that accepts unlimited arguments using rest operator.
112. Use spread operator to add elements in the middle of an array.
113. Demonstrate shallow copy vs deep copy with spread operator.
114. Create a function that merges multiple objects using spread.
115. Use spread operator with destructuring.

---

## Topic 9: DOM Manipulation

### Conceptual Questions
116. What is the DOM (Document Object Model)?
117. How does JavaScript interact with HTML through DOM?
118. What are DOM selectors?
119. Explain the difference between `getElementById` and `querySelector`.
120. What is the difference between `innerHTML` and `textContent`?

### Practical Questions
121. Select an element by ID and change its content.
122. Use `querySelector` to select elements by class.
123. Create a new element and append it to the DOM.
124. Change the style of an element using JavaScript.
125. Remove an element from the DOM.

### Advanced Questions
126. Demonstrate the difference between `createElement` and `innerHTML`.
127. Create a dynamic list that adds items on button click.
128. Build a todo list with add and delete functionality.
129. Implement a color changer that changes background on button click.
130. Create a form that validates input using DOM manipulation.

---

## Topic 10: Events & Event Handling

### Conceptual Questions
131. What are events in JavaScript?
132. Explain event listeners and event handlers.
133. What is event bubbling and event capturing?
134. What is event delegation?
135. How do you prevent default behavior of events?

### Practical Questions
136. Add a click event listener to a button.
137. Create a form with submit event handler.
138. Demonstrate mouseover and mouseout events.
139. Use keyboard events to detect key presses.
140. Create a program that prevents form submission.

### Advanced Questions
141. Explain `event.preventDefault()` and `event.stopPropagation()`.
142. Implement event delegation on a list of items.
143. Create a drag and drop interface using events.
144. Build a custom context menu using right-click event.
145. Implement debouncing for search input.

---

## Topic 11: Arrays

### Conceptual Questions
146. What are arrays in JavaScript?
147. How do you create arrays in JavaScript?
148. What is the difference between array literal and Array constructor?
149. Are JavaScript arrays homogeneous or heterogeneous?
150. What is array destructuring?

### Practical Questions
151. Create an array and access its elements.
152. Add elements to an array using push() and unshift().
153. Remove elements using pop() and shift().
154. Use array destructuring to extract values.
155. Create a multi-dimensional array.

### Advanced Questions
156. Explain the difference between `splice()` and `slice()`.
157. Demonstrate array spread operator for copying arrays.
158. Use `Array.from()` to create arrays from array-like objects.
159. Create an array using `Array.of()` and explain its use.
160. Implement array flattening using `flat()` and `flatMap()`.

---

## Topic 12: Array Methods - Map, Filter, Reduce

### Conceptual Questions
161. What is the `map()` method and when is it used?
162. Explain the `filter()` method with examples.
163. What does the `reduce()` method do?
164. What is the difference between `forEach()` and `map()`?
165. Can you chain array methods?

### Practical Questions
166. Use `map()` to double all numbers in an array.
167. Filter an array to get only even numbers.
168. Use `reduce()` to sum all numbers in an array.
169. Chain `filter()` and `map()` to process data.
170. Use `forEach()` to log each element of an array.

### Advanced Questions
171. Create a function that uses `reduce()` to find maximum value.
172. Use `map()` to transform array of objects.
173. Implement `filter()` to remove duplicates from array.
174. Use `reduce()` to group objects by property.
175. Chain multiple array methods to process complex data.

---

## Topic 13: Array Sorting & Joining

### Conceptual Questions
176. How does the `sort()` method work in JavaScript?
177. What is the default sorting behavior of arrays?
178. How do you sort numbers correctly in JavaScript?
179. What does the `join()` method do?
180. Explain the `reverse()` method.

### Practical Questions
181. Sort an array of strings alphabetically.
182. Sort an array of numbers in ascending order.
183. Use `join()` to convert array to comma-separated string.
184. Reverse an array using `reverse()` method.
185. Sort an array of objects by a specific property.

### Advanced Questions
186. Create a custom comparator function for sorting.
187. Sort an array in descending order.
188. Implement case-insensitive string sorting.
189. Sort an array of dates.
190. Use `localeCompare()` for string sorting.

---

## Topic 14: String Methods

### Conceptual Questions
191. What are strings in JavaScript?
192. Are strings mutable or immutable in JavaScript?
193. What is the difference between `charAt()` and bracket notation?
194. How do template literals improve string handling?
195. What is string interpolation?

### Practical Questions
196. Use `toUpperCase()` and `toLowerCase()` methods.
197. Extract substring using `substring()` and `slice()`.
198. Find the index of a character using `indexOf()`.
199. Replace text in a string using `replace()`.
200. Split a string into array using `split()`.

### Advanced Questions
201. Use `trim()`, `trimStart()`, and `trimEnd()` methods.
202. Implement `startsWith()` and `endsWith()` for validation.
203. Use `padStart()` and `padEnd()` for formatting.
204. Demonstrate `repeat()` method with examples.
205. Use regular expressions with `match()` and `search()`.

---

## Topic 15: Callback Functions

### Conceptual Questions
206. What is a callback function?
207. Why are callbacks important in JavaScript?
208. What is callback hell?
209. How do callbacks enable asynchronous programming?
210. What is the difference between synchronous and asynchronous callbacks?

### Practical Questions
211. Create a function that accepts a callback.
212. Use callback in `setTimeout()`.
213. Implement a custom `forEach()` using callbacks.
214. Create a function that executes callback after delay.
215. Demonstrate callback with array methods.

### Advanced Questions
216. Explain callback hell with nested callbacks example.
217. Create a function that chains multiple callbacks.
218. Implement error-first callback pattern.
219. Use callbacks for event handling.
220. Demonstrate the problem of callback hell and its solution.

---

## Topic 16: Promises

### Conceptual Questions
221. What are Promises in JavaScript?
222. What are the three states of a Promise?
223. How do Promises solve callback hell?
224. What is Promise chaining?
225. Explain `Promise.all()` and `Promise.race()`.

### Practical Questions
226. Create a simple Promise that resolves after 2 seconds.
227. Use `.then()` and `.catch()` with Promises.
228. Chain multiple Promises together.
229. Create a Promise that rejects with an error.
230. Use `Promise.all()` to wait for multiple Promises.

### Advanced Questions
231. Implement a function that returns a Promise.
232. Convert callback-based function to Promise-based.
233. Use `Promise.race()` to implement timeout.
234. Create a Promise-based API call simulator.
235. Implement Promise.allSettled() use case.

---

## Topic 17: Async/Await

### Conceptual Questions
236. What is async/await in JavaScript?
237. How does async/await improve Promise handling?
238. What does the `async` keyword do?
239. What does the `await` keyword do?
240. Can you use await without async?

### Practical Questions
241. Create an async function that waits for a Promise.
242. Use try-catch with async/await for error handling.
243. Convert Promise chain to async/await.
244. Create multiple async operations in sequence.
245. Run multiple async operations in parallel.

### Advanced Questions
246. Explain the difference between Promise.all() and sequential await.
247. Implement error handling in async/await.
248. Create an async function that retries on failure.
249. Use async/await with fetch API.
250. Demonstrate async iteration with for-await-of.

---

## Topic 18: JavaScript with HTML Attributes

### Conceptual Questions
251. How does JavaScript interact with HTML attributes?
252. What is the difference between `getAttribute()` and property access?
253. How do you set HTML attributes using JavaScript?
254. What are data attributes and how to access them?
255. How do you remove attributes using JavaScript?

### Practical Questions
256. Get and set the `src` attribute of an image.
257. Change the `href` attribute of a link.
258. Add and remove `disabled` attribute from a button.
259. Access custom data attributes using `dataset`.
260. Toggle the `class` attribute of an element.

---

## Topic 19: JavaScript with CSS

### Conceptual Questions
261. How does JavaScript manipulate CSS?
262. What is the difference between `style` property and `classList`?
263. How do you add/remove CSS classes using JavaScript?
264. What is `getComputedStyle()`?
265. How do you create CSS animations with JavaScript?

### Practical Questions
266. Change the background color of an element using JavaScript.
267. Add and remove CSS classes using `classList`.
268. Toggle a class on button click.
269. Get computed styles of an element.
270. Create a theme switcher (light/dark mode).

---

## Comprehensive Application Questions

271. Build a calculator using JavaScript.
272. Create a todo list with add, delete, and mark complete features.
273. Implement a countdown timer.
274. Build a simple quiz application.
275. Create an image slider/carousel.
276. Implement form validation with custom error messages.
277. Build a weather app that fetches data from API.
278. Create a shopping cart with add/remove items.
279. Implement a search filter for a list of items.
280. Build a modal popup with open/close functionality.
281. Create a tabs component with content switching.
282. Implement an accordion component.
283. Build a pagination system for large datasets.
284. Create a drag and drop task board.
285. Implement a real-time character counter for textarea.
286. Build a password strength checker.
287. Create a tip calculator.
288. Implement a BMI calculator.
289. Build a random quote generator.
290. Create a color picker tool.

---

## Debugging & Best Practices

291. How do you debug JavaScript code?
292. What are common JavaScript errors and how to fix them?
293. What is strict mode in JavaScript?
294. How do you handle errors in JavaScript?
295. What are JavaScript coding best practices?
296. How do you optimize JavaScript performance?
297. What is the difference between `==` and `===`?
298. How do you avoid global variables?
299. What is the module pattern in JavaScript?
300. How do you write clean and maintainable JavaScript code?

---

**Total Questions: 300**
**Difficulty Levels:** Basic (1-100), Intermediate (101-200), Advanced (201-300)
