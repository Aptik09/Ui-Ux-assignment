# Question Bank: HTML & CSS
## UCS542: UI & UX SPECIALIST

---

## Topic 1: Introduction to HTML

### Conceptual Questions
1. What is HTML and why is it called a markup language?
2. Explain the difference between HTML and HTML5.
3. What is the purpose of DOCTYPE declaration in HTML?
4. How does a browser interpret HTML code?
5. What are the advantages of using semantic HTML?

### Practical Questions
6. Create a basic HTML document structure with all essential elements.
7. Write HTML code to display "Hello World" in a browser.
8. What happens if you don't close an HTML tag? Demonstrate with an example.
9. Create an HTML page that displays your resume.
10. How would you add comments in HTML? Why are they important?

---

## Topic 2: HTML Page Structure

### Conceptual Questions
11. Explain the purpose of `<head>` and `<body>` tags.
12. What is the difference between `<title>` and `<h1>` tags?
13. Why is the `<meta>` tag important for web pages?
14. What is the purpose of the `<link>` tag in HTML?
15. Explain the role of `<script>` tag placement in HTML.

### Practical Questions
16. Create an HTML page with proper head section including meta tags for SEO.
17. Build a webpage structure with header, nav, main, aside, and footer sections.
18. Write HTML code to link an external CSS file and JavaScript file.
19. Create a webpage with proper viewport settings for mobile responsiveness.
20. Demonstrate the use of `<base>` tag in HTML.

---

## Topic 3: HTML Elements

### Conceptual Questions
21. What are block-level and inline elements? Give 5 examples of each.
22. Explain the difference between `<div>` and `<span>`.
23. What are self-closing tags? List 5 examples.
24. What is the purpose of semantic HTML5 elements?
25. Explain the difference between `<section>`, `<article>`, and `<div>`.

### Practical Questions
26. Create a webpage using only semantic HTML5 elements.
27. Build a blog post layout using `<article>`, `<header>`, `<footer>`, and `<section>`.
28. Demonstrate the use of `<figure>` and `<figcaption>` elements.
29. Create a navigation menu using `<nav>` and `<ul>` elements.
30. Write HTML code to embed a video using `<video>` tag with multiple sources.

---

## Topic 4: HTML Attributes

### Conceptual Questions
31. What are HTML attributes and how do they enhance elements?
32. Explain the difference between `id` and `class` attributes.
33. What is the purpose of the `data-*` attribute?
34. How does the `alt` attribute improve web accessibility?
35. What are Boolean attributes in HTML? Give examples.

### Practical Questions
36. Create an image gallery with proper `alt`, `title`, and `loading` attributes.
37. Build a form with various input types and their specific attributes.
38. Demonstrate the use of `data-*` attributes for storing custom data.
39. Create links with `target`, `rel`, and `download` attributes.
40. Write HTML code using `aria-*` attributes for accessibility.

---

## Topic 5: HTML Forms

### Conceptual Questions
41. What is the purpose of HTML forms?
42. Explain the difference between GET and POST methods.
43. What is the role of the `action` and `method` attributes in forms?
44. How does form validation work in HTML5?
45. What is the difference between `name` and `id` attributes in form elements?

### Practical Questions
46. Create a registration form with name, email, password, and submit button.
47. Build a contact form with validation using HTML5 attributes.
48. Create a form with radio buttons, checkboxes, and dropdown menus.
49. Demonstrate the use of `<fieldset>` and `<legend>` in forms.
50. Build a file upload form with multiple file selection.

### Advanced Questions
51. Create a form with `required`, `pattern`, and `placeholder` attributes.
52. Build a multi-step form using HTML and basic CSS.
53. Demonstrate form validation using `novalidate` attribute.
54. Create a search form with autocomplete functionality.
55. Build a feedback form with textarea and rating system.

---

## Topic 6: HTML5 Features

### Conceptual Questions
56. What are the new semantic elements introduced in HTML5?
57. Explain the purpose of `<canvas>` element.
58. What is the difference between `<audio>` and `<video>` tags?
59. How does HTML5 localStorage differ from sessionStorage?
60. What are Web Workers in HTML5?

### Practical Questions
61. Create a webpage using HTML5 semantic elements (header, nav, main, aside, footer).
62. Build an audio player using `<audio>` tag with controls.
63. Demonstrate the use of `<canvas>` to draw shapes.
64. Create a form using new HTML5 input types (email, date, color, range).
65. Build a progress bar using `<progress>` element.

### Advanced Questions
66. Implement drag and drop functionality using HTML5.
67. Create a geolocation-based application using HTML5 API.
68. Build a responsive image gallery using `<picture>` element.
69. Demonstrate the use of `contenteditable` attribute.
70. Create a webpage with embedded SVG graphics.

---

## Topic 7: CSS Basics

### Conceptual Questions
71. What is CSS and why is it used?
72. Explain the CSS box model with a diagram.
73. What is the difference between padding and margin?
74. How does CSS specificity work?
75. What is the cascade in CSS?

### Practical Questions
76. Create an external CSS file and link it to an HTML page.
77. Style a paragraph with different font properties.
78. Demonstrate inline, internal, and external CSS methods.
79. Create a colored box using padding, margin, and border.
80. Style a list to remove default bullets and add custom styling.

---

## Topic 8: CSS Selectors

### Conceptual Questions
81. What are the different types of CSS selectors?
82. Explain the difference between class and ID selectors.
83. What are pseudo-classes and pseudo-elements?
84. How do attribute selectors work in CSS?
85. What is the universal selector and when should it be used?

### Practical Questions
86. Select all paragraphs inside a div with class "container".
87. Style the first child and last child of a list differently.
88. Create a hover effect using pseudo-class.
89. Use attribute selector to style all links that open in new tab.
90. Demonstrate the use of `:nth-child()` selector.

### Advanced Questions
91. Create a zebra-striped table using `:nth-child()`.
92. Style form inputs based on their validation state using pseudo-classes.
93. Use combinators (>, +, ~) to create complex selectors.
94. Demonstrate the difference between `:first-child` and `:first-of-type`.
95. Create a custom checkbox using pseudo-elements.

---

## Topic 9: CSS3 Features

### Conceptual Questions
96. What are the new features introduced in CSS3?
97. Explain CSS3 transitions and animations.
98. What are CSS3 transforms?
99. How do CSS3 gradients work?
100. What is the purpose of CSS3 media queries?

### Practical Questions
101. Create a smooth transition effect on button hover.
102. Build a rotating animation using CSS3 transforms.
103. Create a linear gradient background.
104. Implement a fade-in animation for page elements.
105. Design a card with box-shadow and border-radius.

### Advanced Questions
106. Create a responsive navigation menu using CSS3 media queries.
107. Build a loading spinner using CSS3 animations.
108. Implement a 3D card flip effect using CSS3 transforms.
109. Create a parallax scrolling effect using CSS3.
110. Design an animated hamburger menu icon.

---

## Topic 10: CSS Positioning & Display

### Conceptual Questions
111. Explain the different CSS position values (static, relative, absolute, fixed, sticky).
112. What is the difference between `display: block` and `display: inline`?
113. How does `display: flex` work?
114. What is the purpose of `z-index`?
115. Explain the difference between `visibility: hidden` and `display: none`.

### Practical Questions
116. Create a fixed navigation bar that stays at the top while scrolling.
117. Build a modal dialog using absolute positioning.
118. Demonstrate the use of relative and absolute positioning together.
119. Create a sticky sidebar that follows the scroll.
120. Build a layout using `display: flex`.

### Advanced Questions
121. Create a responsive grid layout using CSS Grid.
122. Build a masonry layout using Flexbox.
123. Implement a sticky footer that stays at the bottom.
124. Create a centered modal overlay using positioning.
125. Design a complex layout with nested flexbox containers.

---

## Topic 11: CSS Alignment

### Conceptual Questions
126. How do you center an element horizontally in CSS?
127. What are the different methods to center an element vertically?
128. Explain the `text-align` property and its values.
129. How does `margin: auto` work for centering?
130. What is the difference between `align-items` and `justify-content` in Flexbox?

### Practical Questions
131. Center a div horizontally and vertically on the page.
132. Create a centered card layout using Flexbox.
133. Align text to the right within a container.
134. Build a centered navigation menu.
135. Create a vertically centered hero section.

---

## Topic 12: CSS Tables

### Conceptual Questions
136. How do you style HTML tables using CSS?
137. What is the purpose of `border-collapse` property?
138. How can you create alternating row colors in a table?
139. What is the difference between `table-layout: auto` and `table-layout: fixed`?
140. How do you make tables responsive?

### Practical Questions
141. Create a styled table with borders and padding.
142. Build a zebra-striped table using CSS.
143. Style table headers differently from table data.
144. Create a responsive table that scrolls horizontally on mobile.
145. Design a pricing table with hover effects.

---

## Topic 13: CSS Buttons

### Conceptual Questions
146. What are the best practices for styling buttons?
147. How do you create button states (hover, active, disabled)?
148. What is the purpose of `cursor: pointer` on buttons?
149. How can you make buttons accessible?
150. What are the differences between `<button>`, `<input type="button">`, and `<a>` styled as button?

### Practical Questions
151. Create a primary button with hover and active states.
152. Build a button group with multiple buttons.
153. Design a ghost button (outline only).
154. Create a disabled button with reduced opacity.
155. Build an icon button with Font Awesome or SVG.

### Advanced Questions
156. Create an animated button with ripple effect.
157. Build a toggle button (on/off switch).
158. Design a floating action button (FAB).
159. Create a button with loading spinner.
160. Build a split button with dropdown.

---

## Comprehensive Application Questions

161. Create a complete landing page using HTML5 and CSS3 with responsive design.
162. Build a portfolio website with multiple sections and smooth scrolling.
163. Design a restaurant menu page with images, descriptions, and prices.
164. Create a blog layout with sidebar, main content area, and footer.
165. Build a product showcase page with grid layout and hover effects.
166. Design a login/signup page with form validation styling.
167. Create a dashboard layout with navigation, sidebar, and content area.
168. Build a timeline component using HTML and CSS.
169. Design a pricing comparison table with featured plan highlight.
170. Create a photo gallery with lightbox effect using CSS.

---

## Debugging & Best Practices

171. How do you debug CSS issues in browser DevTools?
172. What are CSS naming conventions (BEM, OOCSS, SMACSS)?
173. How do you optimize CSS for performance?
174. What is the importance of CSS reset or normalize?
175. How do you ensure cross-browser compatibility?
176. What are CSS preprocessors (SASS, LESS) and their benefits?
177. How do you organize CSS files in large projects?
178. What is the mobile-first approach in CSS?
179. How do you handle CSS specificity conflicts?
180. What are the best practices for writing maintainable CSS?

---

**Total Questions: 180**
**Difficulty Levels:** Basic (1-60), Intermediate (61-120), Advanced (121-180)
